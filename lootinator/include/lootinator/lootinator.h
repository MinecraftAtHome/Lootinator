#ifndef LOOTINATOR_LOOTINATOR_H
#define LOOTINATOR_LOOTINATOR_H

#include <vector>
#include <string>

namespace loot {
    // std::vector<kgen::ConfiguredKernel> get_kernels_from_constraints(std::vector<loot::Constraint> &constraints);
    // std::vector<kgen::ConfiguredKernel> get_kernels_from_constraint_file(std::string constraint_filepath, std::string loot_table_filepath);
}

#endif
