pool 0
{
    lcg-advance 1;
    roll 0
    {
        //...
        case 3
        {
            
        }
        //...
    }
    assert item(minecraft:iron_ingot) >= item-count(14);
    lcg-reset;
    succeed;
}